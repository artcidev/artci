apiVersion: v1
kind: Service
metadata:
  name: {{ include "artci.fullname" . }}
  labels:
    {{- include "artci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "artci.selectorLabels" . | nindent 4 }}
